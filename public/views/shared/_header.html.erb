<section id="header">
  <div class="d-flex row align-items-center px-4">
    <div class="col-sm-9">
      <p class="mb-2 h1">
        <% unless request.original_fullpath == '/' %>
          <a title="Return to homepage" href="<%= AppConfig[:public_proxy_url] %>">
          <% end %>
          FIT SPARC Finding Aids
          <% unless request.original_fullpath == '/' %>
          </a>
        <% end %>
      </p>
    </div>
  </div>
</section>
